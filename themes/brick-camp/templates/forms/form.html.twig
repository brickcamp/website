{% extends 'forms/default/form.html.twig' %}

{% block inner_markup_buttons_start %}
  <div class="form-group {{ form.name == page.header.filter ? 'form-group col-8 col-md-3 mx-auto' }}">
{% endblock %}

{% block button_classes %}
class="{{ button.classes|default('btn btn-primary btn-block') }}"
{% endblock %}