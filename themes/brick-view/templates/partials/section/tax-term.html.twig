<div class="term tax-{{ tax }} term-{{ term }} pure-u-1-3">
    <a href="{{ post.url ~ "/" ~ tax ~ config.system.param_sep ~ term }}">
        <img src="{{ brick_term_image(tax, term).resize(100, 100).url }}" alt={{ term }} class="pure-img">
        <span class="name">{{ term|humanize|title }}</span>
    </a>
</div>